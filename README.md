# Hidive captions hider Chrome extension
A Chrome extension to hide the subtitle captions on Hidive.

# Installation
Go to settings -> extensions -> enable developer mode -> load unpacked -> select this folder
